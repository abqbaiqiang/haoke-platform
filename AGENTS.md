# AGENTS.md — 松茂经营管理平台 Codex 开发规则

本文件对整个仓库生效。任何 Codex/开发 Agent 在修改代码前必须阅读 `docs/00_项目文档索引.md` 并按顺序理解相关文档。

## 1. 项目铁律

1. **不替代精斗云。** V1 不实现精斗云已有的开单、采购、库存、财务记账核心 ERP 功能。
2. **不重复录入事实。** 实际成交、商品、采购、库存等以精斗云导入为准；CRM 只记录过程。
3. **指标口径唯一。** `docs/04_V1指标字典.md` 是 BI 计算的唯一真相，不得擅自改公式。
4. **AI 不算核心数字。** AI 只能解释程序已计算的数据，不得覆盖销售额、毛利、同比环比等。
5. **后端做权限。** 不得依赖“前端隐藏按钮”实现数据安全。
6. **数据不无痕删除。** 核心业务数据软删除/停用；转交、目标、跟进修改等必须留痕。
7. **导入必须幂等。** 同一来源数据重复导入不得导致销售额/订单翻倍。
8. **Docker 可迁移。** 不依赖绿联 NAS 私有 API/路径；相同代码可运行在标准 Linux/云服务器。
9. **配置不写死。** URL、路径、密码、Secret、企业ID等全部通过配置/环境变量/后台设置。
10. **测试是完成条件。** 新功能必须带测试；测试未通过不得声称完成。

## 2. 文档优先级

冲突时按以下优先级执行：

1. `AGENTS.md`
2. `docs/04_V1指标字典.md`（指标） / `docs/03_V1数据字典.md`（字段）
3. `docs/05_页面与权限矩阵.md`
4. `docs/01_PRD.md`
5. 其他工程文档

如仍有无法解决的业务歧义：

- 不要偷偷发明规则；
- 优先做成“后台可配置参数”；
- 或在实现计划中明确标记 `BLOCKED_BUSINESS_DECISION`；
- 不因为一个未决问题阻塞无关模块。

## 3. 每次任务工作流

每个任务必须按以下顺序：

1. 阅读相关文档与现有代码；
2. 输出简短实施计划；
3. 只改本任务必要范围；
4. 编写/更新 migration（如有 schema 变化）；
5. 编写/更新测试；
6. 运行相关测试；
7. 自检权限、幂等、金额精度、数据迁移；
8. 修复失败；
9. 运行当前里程碑测试集；
10. 更新 `CHANGELOG.md` 和任务状态；
11. 报告：改了什么、测试结果、风险/遗留。

不要在同一个任务里顺手重构半个仓库。

## 4. 代码与架构

- 前端：Next.js + TypeScript；
- 后端：FastAPI + SQLAlchemy + Pydantic；
- DB：PostgreSQL；
- migration：Alembic；
- 图表：ECharts；
- 部署：Docker Compose；
- 后台任务：worker 服务；V1 不强制 Redis。

要求：

- 强类型 DTO；
- 业务逻辑不要堆在路由/controller；
- 导入解析器、标准化、持久化分层；
- Connector 与核心业务解耦；
- 统一错误结构；
- 所有金额使用 Decimal/NUMERIC；
- 日期时区明确使用 Asia/Shanghai 展示。

## 5. 数据库变更

- 禁止直接手工改生产表；
- 所有变更必须 Alembic；
- migration 必须从空库可执行；
- 改唯一键/删除列等高风险变更前必须说明迁移影响；
- 不得清空/重建表来“解决”正式数据问题。

## 6. 权限

每个需要数据权限的 API 都要测试：

- owner all；
- manager team；
- sales self；
- finance authorized scope；
- admin system scope。

至少对一个资源写越权测试（S1 尝试读取/修改 S2 数据）并验证 403/404 策略一致。

## 7. 导入

每种导入都必须：

- 保存原文件；
- 计算 SHA256；
- 创建 import_batch；
- 预检；
- 输出错误行；
- 幂等 Upsert；
- 保留 source key 和 batch 追溯；
- 导入后刷新相应聚合。

严禁直接从 Excel 在前端临时算 Dashboard。

## 8. BI

- 实现指标时在代码中保留 metric code；
- 单元测试必须用固定样本验证公式；
- 基期0/缺失按指标字典处理；
- 当前未完月必须标识截至日期；
- 经营销售额和财务营业收入不得混名。

## 9. 安全

- 密码只存哈希；
- API Key/AppSecret 不写入 Git、不输出日志、不明文回显；
- 上传文件检查类型/大小/路径；
- PostgreSQL 不对公网开放；
- 生产关闭 debug；
- 不把真实公司原始数据提交公共仓库。

## 10. 测试与完成定义

参考 `docs/09_测试与验收标准.md`。

Definition of Done：

- 功能完成；
- 自动测试完成并通过；
- 权限验证完成；
- migration 完成；
- 文档/README 如需更新已更新；
- 无 P0/P1 已知缺陷；
- 可通过 Docker 启动。

## 11. 当前开发状态

M0–M5 已全部完成并通过验收；销售员端 V1.1（执行闭环、客户分层、过程指标）已上线，项目处于迭代维护阶段。

新会话首次进入仓库：**不要重做任何已完成里程碑**。先阅读 `AI_RULES.md` 与 `TASK_STATUS.md` 了解现状，从用户当前指定的任务开始。第 1 节的铁律继续有效：不替代精斗云、不做库存/财务记账、AI 不算核心数字、任务范围外的大重构仍然禁止。

## 12. UI 规范化阶段约束（2026-09-16 老板确认，长期有效）

本项目已进入 **UI 规范化阶段**。因仍有页面布局与业务能力待调整，**现在禁止做全项目最终 UI 重构**。当前目标是：建立全项目统一的 UI Design System，所有新增页面、修改页面、重新布局页面从现在起必须遵守该规范。

### 12.1 双端布局独立，视觉语言统一

销售员端和管理者端可以拥有不同的信息架构和页面布局（两个角色的业务目标不同），但**必须使用同一套视觉设计语言**。

### 12.2 统一范围

以下内容统一管理，且**优先通过 `globals.css` 中的 Design Tokens / CSS Variables 定义和引用**：

- 全局字体、字号体系、字重、行高
- 品牌主色、文字颜色、页面背景、卡片背景、边框颜色、状态颜色
- 圆角、阴影
- 页面 Padding、模块间距、卡片 Padding
- Button 高度和样式、Input / Select 高度
- Table 基础视觉、Tabs、Badge / Tag
- 图标尺寸和风格

### 12.3 执行要求

- 新修改的销售员页面、管理者页面，以及新增页面，必须遵守该规范。
- **禁止**每个页面自行重新发明字号、颜色、圆角、按钮尺寸和间距体系。
- 禁止在页面代码/局部 CSS 中写死上述视觉值；确需新值时，先补充 Design Token 再引用。

### 12.4 本阶段禁止事项

- 不为了统一 UI 而大规模重构已完成且暂时不需要修改的页面。
- 不修改业务逻辑、权限、API、数据库、路由。
- 不为了统一视觉而改变业务模块结构。

当前阶段定位：**"建立规范 + 新修改页面开始执行"**，而不是"立即全项目大规模 UI 重构"。

### 12.5 最终收尾：FULL UI CONSISTENCY AUDIT

待销售员端、管理者端的页面布局、业务模块和驾驶舱全部定稿之后，执行最后一次全项目 UI 一致性审计，统一处理遗留的：

字体不一致、字号不一致、颜色不一致、圆角不一致、间距不一致、控件尺寸不一致、卡片样式不一致、表格样式不一致、页面左右对齐不一致、局部写死样式、与 Design Tokens 不一致的遗留代码。

最终形成：**视觉统一、但针对不同角色具有不同业务布局的完整系统**。
