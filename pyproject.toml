[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["apps/backend"]
markers = ["integration: requires dedicated PostgreSQL test database", "unit: isolated unit test"]

[tool.ruff]
line-length = 120
target-version = "py314"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
