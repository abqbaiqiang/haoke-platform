# AI 开发与代码维护规则

本项目已经经过多个 AI 编程工具多轮开发和修改，包括 Codex、Claude Code、Z Code 等。

当前首要目标不是追求开发速度，而是在保持现有业务功能稳定的前提下，控制技术债，避免重复实现、架构混乱和无序重构。

所有 AI 在修改本项目前，必须先阅读：

* README.md
* PRD.md
* ARCHITECTURE.md
* AI_RULES.md
* 以及与当前任务相关的现有代码

## 一、基本原则

1. 现有业务逻辑优先。
2. 不得为了“代码更漂亮”而随意改变现有功能。
3. 不得为了减少代码行数而过度抽象。
4. 不得在没有充分理解现有实现的情况下重新实现已有功能。
5. 不得大范围修改与当前任务无关的模块。
6. 每次修改应尽量小范围、可验证、可回滚。

## 二、新增代码前必须执行

在新增组件、Service、Hook、工具函数、接口、类型、数据库访问逻辑之前：

必须先搜索整个项目，确认是否已经存在相同或类似实现。

如果已有实现能够复用：

优先复用或合理扩展现有实现。

禁止因为“写一个新的更方便”而产生：

* 重复组件
* 重复 Service
* 重复 API
* 重复类型
* 重复工具函数
* 重复业务逻辑

## 三、架构约束

必须保持清晰的模块边界。

每个模块应尽量满足：

* 单一职责
* 高内聚
* 低耦合
* 清晰的数据流
* 清晰的依赖方向

禁止创建一个承担大量无关职责的万能 Service、万能组件或万能工具文件。

如果发现现有架构存在问题，应先记录问题，不得擅自进行大范围重构。

## 四、修改现有功能时

任何修改前先回答：

1. 当前功能由哪些文件实现？
2. 当前数据从哪里来？
3. 当前有哪些模块依赖它？
4. 是否存在公共组件或公共逻辑？
5. 修改是否会影响其他页面或模块？

确认后再修改。

禁止因为修改一个页面而顺带重写整个模块。

## 五、数据库与接口

除非任务明确要求，否则：

* 不修改数据库结构
* 不删除字段
* 不改变字段语义
* 不修改现有 API 契约
* 不改变现有接口返回结构

确实需要修改时，必须先说明影响范围。

## 六、代码清理原则

允许清理：

* 明确未使用代码
* 重复逻辑
* 无用依赖
* 调试代码
* 已确认废弃代码
* 明显重复的组件和工具函数

但删除前必须确认：

不存在其他模块依赖。

禁止根据文件名、注释或表面判断直接删除。

## 七、重构规则

禁止一次性进行全项目大重构。

重构必须按照：

一个模块
→ 修改
→ 检查
→ 测试
→ 构建
→ 确认
→ 再处理下一个模块

每次重构都必须说明：

* 修改了什么
* 为什么修改
* 是否改变业务行为
* 影响哪些文件
* 存在哪些风险

## 八、验证要求

代码修改后根据项目实际情况执行：

* build
* lint
* typecheck
* test

如果项目缺少某类自动化测试，应明确说明，而不是默认代码正确。

## 九、禁止事项

禁止：

* 未搜索现有代码就新增重复实现
* 擅自重新设计现有架构
* 一次修改大量无关文件
* 为追求“简洁”牺牲可读性
* 为减少代码行数制造复杂抽象
* 擅自修改 UI
* 擅自修改业务规则
* 擅自改变数据库结构
* 擅自升级核心依赖
* 删除不了解用途的代码
* 隐藏错误或忽略测试失败

## 十、AI 工作原则

开发任务中：

先理解，再修改。

重构任务中：

先审计，再修改。

不确定时：

记录问题，不擅自猜测。

项目稳定性优先于代码形式上的“漂亮”。
