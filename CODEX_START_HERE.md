# 第一次交给 Codex 的执行指令

将下列内容作为第一次任务指令使用：

> 你是“松茂经营管理平台”的主开发 Agent。先阅读根目录 `AGENTS.md`，再按 `docs/00_项目文档索引.md` 的顺序阅读全部项目文档。不要立即实现整套系统。
>
> 当前只允许执行 **M0：工程骨架**，范围严格以 `docs/08_开发任务拆解.md` 的 M0 为准。
>
> 第一步先输出：
> 1. 你对系统边界的理解；
> 2. M0 技术实施计划；
> 3. 计划创建的目录/服务；
> 4. M0 测试清单；
> 5. 你发现的文档冲突或阻塞项。
>
> 确认计划后开始实现 M0。每完成一部分立即补测试。M0 完成后必须运行完整 M0 测试、自检 Docker 持久化和角色权限，修复失败，更新 README/CHANGELOG，然后输出测试结果和 M0 验收清单。
>
> **里程碑状态：M0–M5 已全部完成并验收，销售员端 V1.1 已上线。** 不要重做已完成的里程碑；从 `TASK_STATUS.md` 和用户当前指令出发，小步迭代。

## M0 验收后下一条指令

> 根据 `docs/08_开发任务拆解.md` 和 `docs/09_测试与验收标准.md`，复核 M0 退出条件。先修复所有 P0/P1 和测试失败。全部通过后，给出 M1 计划，但不要在计划未审阅前扩展到 M2。

## 后续每阶段通用指令

> 只执行当前里程碑。阅读对应数据/指标/权限文档，先计划，再实现，再测试，再自检，再修复。任何业务口径不明确时不要自行发明，标注为可配置项或阻塞决策。完成后提供：改动清单、migration、测试结果、已知问题、验收清单。不要自行进入下一里程碑。
