# C1–C4 结构化改造 · 执行任务书

- 日期：2026-09-16
- 依据：`ARCHITECTURE_AUDIT.md`（外部审计，已逐条核实属实）§十「建议的重构顺序」
- 前置状态：全部工作已提交（最新见 `git log`），基线标签 `v1.1-sales`；`/health` 已动态读迁移 head；`AI_RULES.md` 生效
- 铁则：每个阶段独立提交、跑全量回归（pytest 259 + `run_e2e.py` E2E 38）、可单独回滚；**顺序不可调换**（C1 护栏未就位前禁止 C4）

## C1 护栏（约 2 小时，先做）
1. `tests/test_m3.py::test_metric_descriptions_match_dictionary` 改双向断言：代码 emit 的指标码 ⊆ 字典码（当前 5 个 emit 码缺目录条目：TGT_QUARTER_AMT/COMPLETION/PROGRESS、SALE_MOM_BASE、SALE_YOY_BASE——先补目录或排除白名单并注明）
2. 全角括号重名回归测试（审计 P1-05）：同一客户名分别以 `（）` 和 `()` 建档，断言 duplicate_flags 能命中
3. `scripts/run_e2e.py` 增加 APP_ENV 检查（非 development 拒绝运行），消除对生产库误跑 DELETE 的风险（审计 P2-28）
4. `storage_init.py` 最小单测（审计 P1-15）

## C2 前端公共层（约 1 天，每步一次 E2E 回归）
1. `apps/frontend/app/lib/format.ts`：统一 money（空值"—"、支持负数、¥前缀）、日期/北京时间换算；迁移 8 处金额、7 处时间
2. `lib/api.ts`：统一 api + useData（保留 AbortController 语义）；迁移 6 处
3. `lib/types.ts`：从后端 schema 镜像 Role/Customer/Task/Followup/Opportunity/Metric 等；**补齐已漂移字段**（Contact.decision_role 的 user/introducer、customer_status、is_effective、priority、AttentionPage.counts）
4. `lib/labels.ts`：角色/阶段/枚举中译收敛

## C3 后端公共层（约 1 天）
1. `app/deps.py` 收敛 Actor/DB（消除 sales_workspace→crm_api 反向依赖与 main.py 末端 noqa 导入，审计 P2-09）
2. `app/constants.py`：角色/状态/等级/指标码常量（P3-08）
3. 时区统一读 `settings.app_timezone`，删 4 处硬编码 `Asia/Shanghai`（P2-03，仅改 4 行）
4. 修复 `bi_service.py:819` 可能为 None 的链式取属性（P2-08）

## C4 大文件拆分（2~3 天，前置：C1 完成）
1. `bi_service.py`(907) 拆为 装载/口径/指标/DTO 四模块，每拆一块跑全量比对
2. 删死 CSS 类约 20 个（审计 §7.2 清单，删前逐类 grep 确认）+ 更新 MANIFEST/版本串等过期元数据（P2-24/P3-07）
3. `sales.tsx`/`crm.tsx` 拆分与超长行格式化（**改到哪、格式化到哪**，不做一次性重排）

## 明确不做（审计 §10.1）
- 不做全项目一次性格式化；不批量回改历史客户数据；不删 `actual_cost_amount` 列；不拆没有护栏的文件；不改 admin 权限口径（2026-09-16 已定：admin=最高权限）
